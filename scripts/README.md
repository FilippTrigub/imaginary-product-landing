# Hello World Scripts

This folder contains simple hello-world scripts:

- `hello.sh` - Bash
- `hello.py` - Python 3
- `hello.js` - Node.js

Run them with `./hello.sh`, `./hello.py`, or `./hello.js` from this directory.
