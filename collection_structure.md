# Collection: testcollection

## Database

testdb

## Schema

The `testcollection` has the following fields:

*   `_id`: ObjectId - Unique identifier for each document.
*   `title`: String - The title of the document.
*   `bool_field`: Boolean - A boolean value.
*   `random_value`: Number - A random numerical value.
*   `normal_value`: Number - A numerical value from a normal distribution.
