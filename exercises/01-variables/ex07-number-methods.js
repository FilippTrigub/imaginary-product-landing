/**
 * Exercise 7: Number Methods
 * 
 * Work with number methods and properties.
 * 
 * Instructions:
 * 1. Create 'rounded' - round the decimal to nearest integer
 * 2. Create 'fixed' - convert decimal to string with 2 decimal places
 * 3. Create 'parsed' - parse the string to an integer
 */

const decimal = 3.14159;
const numberString = "42";

// Write your code below:
const rounded = 0;
const fixed = "";
const parsed = 0;

// Test your code (don't modify):
if (typeof module !== 'undefined') {
  module.exports = { rounded, fixed, parsed };
}
