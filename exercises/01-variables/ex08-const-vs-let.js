/**
 * Exercise 8: const vs let
 * 
 * Understand when to use const vs let.
 * 
 * Instructions:
 * 1. Use const for 'PI' (value won't change)
 * 2. Use let for 'radius' (value might change)
 * 3. Calculate 'area' using the formula: PI * radius * radius
 */

// Write your code below:



// Test your code (don't modify):
if (typeof module !== 'undefined') {
  module.exports = { PI, radius, area };
}
