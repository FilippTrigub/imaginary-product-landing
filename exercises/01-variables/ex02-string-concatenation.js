/**
 * Exercise 2: String Concatenation
 * 
 * Combine strings using concatenation.
 * 
 * Instructions:
 * Create a variable 'greeting' that combines firstName and lastName
 * to create a greeting message: "Hello, [firstName] [lastName]!"
 */

const firstName = "John";
const lastName = "Doe";

// Write your code below:
const greeting = "";

// Test your code (don't modify):
if (typeof module !== 'undefined') {
  module.exports = { greeting };
}
