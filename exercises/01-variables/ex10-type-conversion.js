/**
 * Exercise 10: Type Conversion
 * 
 * Convert between different data types.
 * 
 * Instructions:
 * 1. Convert 'numString' to a number and store in 'numValue'
 * 2. Convert 'numValue2' to a string and store in 'stringValue'
 * 3. Convert 'boolString' to a boolean and store in 'boolValue'
 */

const numString = "123";
const numValue2 = 456;
const boolString = "true";

// Write your code below:
const numValue = 0;
const stringValue = "";
const boolValue = false;

// Test your code (don't modify):
if (typeof module !== 'undefined') {
  module.exports = { numValue, stringValue, boolValue };
}
