# Hello World Scripts

Quick examples you can run locally.

- Node: `node hello/node-hello.js`
- Bash: `bash hello/bash-hello.sh`
- Python: `python3 hello/python-hello.py`

To view the HTML example:

- Open `hello/index.html` in a browser.

