/**
 * Coding Challenges
 *
 * Here are 5 coding challenges in JavaScript.
 */

// Challenge 1: Anagram Checker
// Write a function `areAnagrams(str1, str2)` that takes two strings as input
// and returns `true` if they are anagrams of each other, `false` otherwise.
// Anagrams are words or phrases formed by rearranging the letters of another,
// typically using all the original letters exactly once.
// Example: areAnagrams("listen", "silent") // -> true
// Example: areAnagrams("hello", "world")  // -> false

function areAnagrams(str1, str2) {
  // Your code here
}

// Challenge 2: Find the Longest Word
// Write a function `findLongestWord(sentence)` that takes a sentence (string)
// as input and returns the longest word in that sentence.
// If there are multiple words with the same maximum length, return the first one encountered.
// Punctuation should not be considered part of the word.
// Example: findLongestWord("The quick brown fox jumped over the lazy dog") // -> "jumped"
// Example: findLongestWord("Google Apple Microsoft") // -> "Microsoft"

function findLongestWord(sentence) {
  // Your code here
}

// Challenge 3: Factorial Calculator
// Write a function `factorial(n)` that calculates the factorial of a non-negative integer `n`.
// The factorial of a non-negative integer `n`, denoted as `n!`, is the product of all positive
// integers less than or equal to `n`. 0! is defined as 1.
// Example: factorial(5) // -> 120 (5 * 4 * 3 * 2 * 1)
// Example: factorial(0) // -> 1

function factorial(n) {
  // Your code here
}

// Challenge 4: Palindrome Checker
// Write a function `isPalindrome(str)` that takes a string as input
// and returns `true` if it is a palindrome, `false` otherwise.
// A palindrome is a word, phrase, number, or other sequence of characters
// which reads the same backward as forward, ignoring punctuation, case, and spacing.
// Example: isPalindrome("madam") // -> true
// Example: isPalindrome("A man, a plan, a canal: Panama") // -> true
// Example: isPalindrome("hello") // -> false

function isPalindrome(str) {
  // Your code here
}

// Challenge 5: Fibonacci Sequence
// Write a function `fibonacci(n)` that returns the nth number in the Fibonacci sequence.
// The Fibonacci sequence is a series of numbers where each number is the sum of the two preceding ones,
// usually starting with 0 and 1.
// F(0) = 0, F(1) = 1, F(n) = F(n-1) + F(n-2) for n > 1
// Example: fibonacci(0) // -> 0
// Example: fibonacci(1) // -> 1
// Example: fibonacci(6) // -> 8 (0, 1, 1, 2, 3, 5, 8)

function fibonacci(n) {
  // Your code here
}
